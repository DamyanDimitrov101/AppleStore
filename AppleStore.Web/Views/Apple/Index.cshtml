@model IEnumerable<AppleStore.ViewModels.AppleViewModel>

@{
    ViewBag.Title = "Index";
}
<div class="container px-2 px-lg-1 py-lg-5 my-3 store-index-topMargin">        
        <div class="row row-cols-1 row-cols-lg-3 row-cols-md-3 g-3 d-flex justify-content-center">
            
                @foreach (var item in Model)
                {
                    @Html.Partial("_AppleCard", item);
                }
            </div> 
    </div>